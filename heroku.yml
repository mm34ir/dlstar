build:
  docker:
    web: Dockerfile
    worker: bot/Dockerfile
run:
  web: gunicorn main:main --bind 0.0.0.0:$PORT --timeout 86400 -w 10 --worker-class aiohttp.GunicornWebWorker
  worker: python3 main.py